<%= turbo_frame_tag "main",
src: suggestions_url(person_one_lat: @person_one.lat,
                    person_one_lon: @person_one.lon,
                    person_two_lat: @person_two.lat,
                    person_two_lon: @person_two.lon,
                    token: @token,
                    categories: @categories.join(','),
                    conditions: @conditions) do %>
  <%= render "map" %>
<% end %>
