<h2>SHOW</h2>

<div class="container">
  <p><%= "#{@place_uno.name} -- #{@place_uno.street} -- #{@place_uno.number}" %></p>

  <div style="width: 100%; height: 600px;"
    data-controller="mapbox"
    data-mapbox-people-markers-value="<%= @people_markers.to_json %>"
    data-mapbox-token-value="<%= @token %>">
  </div>
</div>
